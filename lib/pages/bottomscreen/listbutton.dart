import 'package:flutter/material.dart';

class Listbutton extends StatelessWidget {
  const Listbutton({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Center(child: Text("hy"))),
    );
  }
}
